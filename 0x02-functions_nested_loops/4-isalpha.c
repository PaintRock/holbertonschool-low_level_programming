/*
*File: 4-isalpha.c
*main - check the code.
*Auth:  A. Adel Knode
*/


#include <main.h>

/**
* int _islower(int c); Finds lowercase 'c'.
*
* Return: Not Always 0.
*/
int main(void)
{
int _is_lower(int c);
if (c <= 'a' && c >= 'z')
if c <= 'A' && c >= 'Z')
then
return (1);
else
return (0);

}
