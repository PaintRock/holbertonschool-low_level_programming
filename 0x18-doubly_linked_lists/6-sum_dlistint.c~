#include "lists.h"

/**
 *sum_dlistint - sums all the data of a disting_t list
 *@head: the head of the distint_t list.
 *
 *Return: The sum of all of the data.
 */
int sum_dlistint(dlistint_t *head)
{
	int sum = 0;

	while (head)
	{
		sum += head->;
		head = head->;
	}
	return (sum);
}
