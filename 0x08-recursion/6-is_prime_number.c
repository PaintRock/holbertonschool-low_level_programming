#include "main.h"
/**
 * is_prime_number - prints string in reverse
 * @n: string
 * Return: no
 */
int is_prime_number(int n)
{
	if (n < 1)
		return (0);
	_print_rev_recursion((s + 1));
	_putchar(*s);
}
