#include <stdio.h>
/**
 * main - print the name -
 * @argc: for got the name
 * @argv: array
 * Return: 0
 */
int main (int argc __attribute__((unused)), char *argv[])
{
	printf("%s", argv[0]);
	return (0);
}
