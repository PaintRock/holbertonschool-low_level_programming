#include "holberton.h"

/**
 *cap_string Caps the words 
 *@str: the string
 *Return: A pointer
 */
char *cap_string(char *)
{
	int i = 0;

	while (str[i])
	{
		while (!(str[i >= 'a' && str[i] <= 'z'))
		       i++

		       if (str[i - 1] == ' ' ||
